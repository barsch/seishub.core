<h1>Query DB</h1>

<h2>Query</h2>
<form method="post" action="">
  <div class="field">
    <textarea class="monospace" 
              name="query" rows="5" cols="80">$query</textarea>
  </div>
  
  <div class="button">
    <label>SQL Tables</label><br />
    #for $table in $tables
    <input type="submit" name="table" value="$table.replace($prefix, '')" />
    #end for
    <br /><br />
    <label>SQL Views</label><br />
    #for $view in $views
    <input type="submit" name="view" value="$view" />
    #end for
    <br /><br />
    <input type="submit" name="send" value="Query" />
  </div>
</form>

<br />
<br />

<h2>Result</h2>
#if $cols
<p>
  $rows rows<br />
  $clock sec
</p>
<table class="list">
  <tr>
  #for $k in $cols 
    <th>$k</th>
  #end for
  </tr> 
#for $r in $result
  <tr>
  #for $f in $r
    <td>
      #filter WebSafe
      #if isinstance(f, unicode)
        #echo f.__repr__() #
      #else
        $f
      #end if
      #end filter
    </td>
  #end for
  </tr>
#end for
</table>
#end if
