<h1>Query DB</h1>

<h2>Query</h2>
<form method="post" action="">
  <div class="field">
    <textarea name="query" rows="3" cols="80">$query</textarea>
  </div>
  
  <div class="button">
    #for $table in $tables
    <input type="submit" name="$table" value="$table.replace('default_', '')" />
    #end for
    <br /><br />
    <input type="submit" name="send" value="Query" />
  </div>
</form>

<br />
<br />

<h2>Result</h2>
#if $cols
<table class="list">
  <tr>
  #for $k in $cols 
    <th>$k</th>
  #end for
  </tr> 
#for $r in $result
  <tr>
  #for $f in $r
    <td>
      #filter WebSafe
      #if isinstance(f, unicode)
        #echo f.__repr__() #
      #else
        $f
      #end if
      #end filter
    </td>
  #end for
  </tr>
#end for
</table>
#end if
